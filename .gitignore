# PlatformIO
.pio/
.vscode/
.clang_complete
.gcc-flags.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv

# IDE
.idea/
*.swp
*.swo
*~
.DS_Store

# Build artifacts
*.o
*.hex
*.elf
*.bin

# Logs and captures: scratch captures stay local, but the curated
# reference set (protocol ground truth / test fixtures) is tracked
captures/*.txt
!captures/.gitkeep
!captures/reference/

# Analysis outputs
analysis/*.json
analysis/*.csv
analysis/__pycache__/

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.bak
*.log

# KiCad autosave files
_autosave-*

# Node.js / React Router app
app/node_modules/
app/.react-router/
app/build/
app/test-results/
app/playwright-report/
*.lock
tools/kicad-mcp/
tools/kicad-mcp-server/

# Stray local node_modules (no source tracked here)
viewer/
_site/

# Generated build artifacts: regenerated by CI on every PR (ci.yml) and
# published to Pages on every merge (pages.yml). Freeze an order package by
# tagging the commit, not by committing outputs.
hardware/fab/
hardware/esp32-ir-remote_bom.csv
hardware/erc-report.txt
hardware/snapshots/
hardware/esp32-ir-remote.xml
hardware/fabcheck/
